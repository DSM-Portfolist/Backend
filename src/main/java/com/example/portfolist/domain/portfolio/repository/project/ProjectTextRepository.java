package com.example.portfolist.domain.portfolio.repository.project;

import com.example.portfolist.domain.portfolio.entity.project.ProjectText;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProjectTextRepository extends JpaRepository<ProjectText,Long> {
}
