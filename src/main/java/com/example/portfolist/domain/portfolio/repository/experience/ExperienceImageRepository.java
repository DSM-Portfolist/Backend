package com.example.portfolist.domain.portfolio.repository.experience;

import com.example.portfolist.domain.portfolio.entity.experience.ExperienceImage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ExperienceImageRepository extends JpaRepository<ExperienceImage,Long> {
}
