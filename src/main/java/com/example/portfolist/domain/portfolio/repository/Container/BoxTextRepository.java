package com.example.portfolist.domain.portfolio.repository.experience;

import com.example.portfolist.domain.portfolio.entity.container.BoxText;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ExperienceTextRepository extends JpaRepository<BoxText,Long> {
}
